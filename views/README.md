# views/

WaferIQ P4 — first-value results UI.

One workflow, end to end:

- **POS recon:** reconciliation dashboard (matched vs flagged, drill-down
  on discrepancies).
- **Funnel:** funnel-health view (stages, conversion, at-risk wins).

Charts in D3 (existing strength, no chart library). CSV/XLSX export is
table stakes.

Goal: unaided new user reaches useful, exportable result on their own data
in under 10 minutes.

Empty on purpose — populate in P4.
